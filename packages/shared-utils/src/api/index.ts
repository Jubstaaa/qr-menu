import * as admin from "./admin";
import * as publicModule from "./public";
import * as common from "./common";

export { admin, publicModule, common };

export const apiUtils = {
  admin,
  publicModule,
  common,
};
